# Salin file ini ke .env dan isi dengan URL backend Anda
# Untuk development lokal, kosongkan (vite proxy akan handle otomatis)
# Untuk production (Vercel), isi dengan URL Railway Anda
VITE_API_BASE_URL=https://your-backend-url.up.railway.app

